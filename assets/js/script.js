'use strict';function hf(e){e.preventDefault();const b=document.getElementById('fsb');b.textContent='Sending…';b.disabled=true;setTimeout(()=>{b.textContent='✅ Sent!';setTimeout(()=>{b.textContent='Send Message →';b.disabled=false;e.target.reset();},3000);},1500);}
const cur=document.getElementById('cur'),ring=document.getElementById('ring');let mx=0,my=0,rx=0,ry=0;document.addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;cur.style.left=mx+'px';cur.style.top=my+'px';});(function lr(){rx+=(mx-rx)*.1;ry+=(my-ry)*.1;ring.style.left=rx+'px';ring.style.top=ry+'px';requestAnimationFrame(lr);})();document.querySelectorAll('a,button,.pr,.skb,.cb').forEach(el=>{el.addEventListener('mouseenter',()=>{cur.classList.add('h');ring.classList.add('h');});el.addEventListener('mouseleave',()=>{cur.classList.remove('h');ring.classList.remove('h');});});
const pc=document.getElementById('pc'),ctx=pc.getContext('2d',{alpha:false});function resize(){pc.width=innerWidth;pc.height=innerHeight;}resize();addEventListener('resize',resize);
const pts=Array.from({length:60},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.28,vy:(Math.random()-.5)*.28,r:Math.random()*1.4+.4,a:Math.random()*.45+.08,h:Math.random()<.5?265:300}));
(function draw(){ctx.clearRect(0,0,pc.width,pc.height);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=pc.width;if(p.x>pc.width)p.x=0;if(p.y<0)p.y=pc.height;if(p.y>pc.height)p.y=0;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,6.283);ctx.fillStyle=`hsla(${p.h},75%,70%,${p.a})`;ctx.fill();});for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){const dx=pts[i].x-pts[j].x,dy=pts[i].y-pts[j].y,d=Math.hypot(dx,dy);if(d<110){ctx.beginPath();ctx.moveTo(pts[i].x,pts[i].y);ctx.lineTo(pts[j].x,pts[j].y);ctx.strokeStyle=`rgba(124,58,237,${.16*(1-d/110)})`;ctx.lineWidth=.5;ctx.stroke();}}requestAnimationFrame(draw);})();
window.addEventListener('scroll',()=>document.getElementById('nav').classList.toggle('s',scrollY>60));
window.addEventListener('scroll',()=>{const el=document.getElementById('wm');if(el)el.style.transform=`translate(-50%,calc(-50% + ${scrollY*.12}px))`;});
const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');e.target.querySelectorAll('.sfill').forEach(b=>b.style.width=b.dataset.w+'%');}});},{threshold:.08});document.querySelectorAll('.fu,.st').forEach(el=>io.observe(el));document.querySelectorAll('.sfill').forEach(b=>{const o=new IntersectionObserver(en=>{if(en[0].isIntersecting){b.style.width=b.dataset.w+'%';o.disconnect();}},{threshold:.3});o.observe(b);});
window.addEventListener('load',()=>setTimeout(()=>document.getElementById('pre').classList.add('out'),700));
